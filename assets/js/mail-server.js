// steps for setting up mail capabilities on portfolio site:

	// 1) require (npm 'mailgun')

	// 2) start up Express server

	// 3) create endpoint that runs mailgun code

	// 4) on form submit, do a 'POST' request to endpoint

	// 	$.POST('/formSubmit', etc.)
